<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="css/styles.css">
    <title><%=title%></title>
</head>
<body>
    
    <div class="jumbotron bg-cover">
        <h1 class='mb-4' style="color: beige;">Blogger Spot</h1>
    
    
    <div class='container'>
        <a href='/articles/new' class='btn btn-success'>New Article</a>
    

    <% articles.forEach(article=>{%>
    <div class='card mt-4 bg-transparent border border-light'>
        <div class='card-body'>
            <h3 class='card-title' style="color: beige;"><%=article.title%></h3>
            <div class='card-subtitle mb-2' style="color:beige">
                <%=article.date.toLocaleDateString()%>
            </div>
            <div class='card-text mb-2' style="color:white">
                <%=article.description%>
            </div>
            <a href="/articles/<%=article.slug%>" class='btn btn-primary'>Read More</a>
            <form action="/articles/<%=article.id%>?_method=DELETE" method='POST' class='d-inline'>
            <button type='submit' class='btn btn-danger'>Delete</button>
        </form>
        </div>
    </div>
    <%})%>
</div>
</div>

    
</body>
</html>